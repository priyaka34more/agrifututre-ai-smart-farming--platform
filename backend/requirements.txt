# ================================================================
# AgriFuture AI — Production Requirements
# Render deployment: python-3.11-slim
# ================================================================
# REMOVED:
#   torch / torchvision  → loaded from app.state (model .pth file)
#                          kept as optional — install separately if
#                          you need GPU inference on a paid Render plan
#   starlette==1.0.0     → FastAPI bundles its own Starlette; pinning
#                          a separate version breaks the framework
#   annotated-doc==0.0.4 → does not exist on PyPI (typo/phantom pkg)
# ADDED:
#   beautifulsoup4       → required by government_schemes_service.py
#   lxml                 → faster BS4 parser (optional but faster)
# ================================================================

# ── Web framework ────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# ── Database ─────────────────────────────────────────────────
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10
greenlet==3.1.1

# ── Auth / security ──────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
cryptography==43.0.3

# ── Validation ───────────────────────────────────────────────
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0
annotated-types==0.7.0

# ── HTTP / async ─────────────────────────────────────────────
httpx==0.28.1
requests==2.32.3
aiohttp==3.11.10
anyio==4.7.0
h11==0.14.0
httptools==0.6.4
websockets==14.1
watchfiles==1.0.3

# ── Data science ─────────────────────────────────────────────
numpy==1.26.4
pandas==2.2.3
scikit-learn==1.6.0
scipy==1.14.1
statsmodels==0.14.4
joblib==1.4.2
threadpoolctl==3.5.0
patsy==1.0.1

# ── Image processing ─────────────────────────────────────────
pillow==11.0.0
opencv-python-headless==4.10.0.84

# ── ML (PyTorch) ─────────────────────────────────────────────
# Uncomment ONLY on a paid Render plan with enough RAM/disk.
# The free plan (512 MB RAM) will OOM during build.
# torch==2.5.1
# torchvision==0.20.1

# ── Web scraping (government_schemes_service.py) ─────────────
beautifulsoup4==4.12.3
lxml==5.3.0

# ── PDF / reporting ──────────────────────────────────────────
reportlab==4.2.5

# ── Utilities ────────────────────────────────────────────────
python-dotenv==1.0.1
python-dateutil==2.9.0.post0
PyYAML==6.0.2
click==8.1.7
packaging==24.2
six==1.17.0
pyasn1==0.6.1
rsa==4.9
ecdsa==0.19.0
dnspython==2.7.0

# ── Async support ────────────────────────────────────────────
aiohappyeyeballs==2.4.4
aiosignal==1.3.1
attrs==24.2.0
frozenlist==1.5.0
multidict==6.1.0
yarl==1.18.3
propcache==0.2.1
charset-normalizer==3.4.0
idna==3.10
certifi==2024.12.14
urllib3==2.2.3

# ── Misc ─────────────────────────────────────────────────────
typing_extensions==4.12.2
setuptools==75.6.0
tzdata==2024.2
python-jose==3.3.0
Jinja2==3.1.4
MarkupSafe==3.0.2
